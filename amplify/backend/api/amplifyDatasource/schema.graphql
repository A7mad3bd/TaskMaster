input AMPLIFY { globalAuthRule: AuthRule = { allow: public } }


type Task @model {
  id: ID!
  title: String!
  description: String
  status: String
}
type Note @model {
  id: ID!
  content: String!
}

type User @model
{
regnum: ID!
name : String!
  age: Int
number: String
}

type Task @model {
  id: ID!
  title: String
  body:String
  state:String
}
